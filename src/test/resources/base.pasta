; Base application config — stable defaults

@app {
    port: 8080,
    workers: 4,
    log_level: "info",
    debug: false
}

@db {
    engine: "postgres",
    port: 5432,
    pool_min: 2,
    pool_max: 10
}
